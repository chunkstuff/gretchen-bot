# Gretchen - Confession Bot

Discord bot for The Cult of Sir that handles anonymous confessions.

## Setup

1. Copy `.env.example` to `.env`
2. Fill in your Discord credentials
3. Install dependencies: `pip install -r requirements.txt`
4. Run: `python3 bot.py`

## Deployment

Automatically deploys to PiLab on push to main via GitHub Actions.